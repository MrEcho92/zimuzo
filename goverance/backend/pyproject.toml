[project]
name = "goverance-backend"
version = "0.1.0"
description = "Backend services for Goverance application"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.128.0",
    "uvicorn[standard]>=0.12.0,<0.23.0",
]
